<form method="post" action="{{ path('app_admin_forbidden_delete', {'id': forbidden_word.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ forbidden_word.id) }}">
    <button class="btn block w-fit rounded bg-gray-700 text-gray-300 hover:bg-gray-600 hover:text-white px-4 py-1">Delete</button>
</form>
