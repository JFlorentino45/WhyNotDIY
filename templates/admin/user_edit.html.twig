{% extends 'base.html.twig' %}

{% block title %}Edit Profile{% endblock %}

{% block body %}
<div class="flex flex-col mx-auto max-w-fit my-6 mh-auto p-8 bg-gray-200 rounded-md">
    <p class="text-xl font-semibold text-center mt-4 items-center">Edit Profile</p>

    {{ include('admin/user_edit_form.html.twig', {'button_label': 'Update'}) }}

    <button><a class="block w-fit rounded bg-gray-700 text-gray-300 hover:bg-gray-600 hover:text-white px-4 pb-1.5 pt-2 mt-2 font-sans" href="{{ path('app_admin_users') }}">Back to Users</a></button>
</div>
{% endblock %}
