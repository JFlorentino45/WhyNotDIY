{% extends 'base.html.twig' %}

{% block body %}

    <h1>Change Password</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}

    <button type="submit">Change Password</button>
    <button><a href="{{ path('app_user_show', {'id': app.user.id}) }}">Back to Profile</a></button>
    {{ form_end(form) }}
{% endblock %}