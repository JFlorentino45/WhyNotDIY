{% extends 'base.html.twig' %}

{% block title %}ReportsC{% endblock %}

{% block body %}
    <h1>ReportsC</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reports_c.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reports_c_index') }}">back to list</a>

    <a href="{{ path('app_reports_c_edit', {'id': reports_c.id}) }}">edit</a>

    {{ include('reports_c/_delete_form.html.twig') }}
{% endblock %}
